# Backend ORM boundary

Generated Diesel/SeaORM projections and narrow named database capabilities belong here. Existing `src/` code is transition material and should move beneath this boundary only after compatibility tests preserve its public Rust API.
