# Backend ORM implementation

Executable ORM entities, named repositories/operations, and backend database configuration live here. Schema changes are planned/applied by reviewed migration workflows, never ORM startup.
