[package]
name = "zed-orm-core"
version = "0.2.0"
edition = "2021"
description = "Private backend-only SeaORM/Diesel capability boundary for zed-pkg"
repository = "https://github.com/zed-pkg/zed-orm-core"
publish = false

[lib]
name = "zed_orm_core"

[features]
default = ["read-only"]
read-only = []
read-write = ["read-only"]

[dependencies]
sea-orm = { version = "1", default-features = false, features = [
  "sqlx-postgres",
  "runtime-tokio-rustls",
  "macros",
  "with-chrono",
  "with-uuid",
] }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
